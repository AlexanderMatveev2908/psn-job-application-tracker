apiVersion: v1
kind: Service
metadata:
  name: psn-job-application-tracker-client
spec:
  type: NodePort
  selector:
    app: psn-job-application-tracker-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
      nodePort: 30081
